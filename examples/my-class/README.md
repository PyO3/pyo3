# word-count

## Build

```bash
python setup.py install
```

## Usage

```python
from my_class import MyClass

c = MyClass("title")
c.title = "new title"
print("The title of c is {}".format(c.title))
```
