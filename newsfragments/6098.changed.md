The closure invoked by `with_critical_section_mutex2` now accepts an
`Option<EnteredCriticalSection>` instead of an `EnteredCriticalSection`. The
Option will contain None if the two mutexes passed to the function are the same
object and will contain an `EnteredCriticalSection` otherwise.
