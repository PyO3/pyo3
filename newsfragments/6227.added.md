Add `PyContext`, as well as `register_context_watcher!`, `ContextEvent`, and
`ContextWatcherGuard` for watching `contextvars.Context` on GIL-enabled CPython
3.14+. `PyContext` itself is available on all supported CPython versions.
