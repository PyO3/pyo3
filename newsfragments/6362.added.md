`experimental-inspect`: classes which extend their base's instance layout are now decorated with `typing_extensions.disjoint_base` in the generated stubs, following [PEP 800](https://peps.python.org/pep-0800/).
