`experimental-inspect`: classes which extend their base's instance layout are now decorated with `disjoint_base` in the generated stubs, following [PEP 800](https://peps.python.org/pep-0800/). It is imported from `typing` when targeting Python 3.15 or newer and from `typing_extensions` otherwise.
