`pyo3::types` module now has additional submodules, such as `pyo3::types::dict`, which provide helper types for various Python types, similar to how `std::collections::HashMap` has a `std::collections::hash_map` submodule. Helper types exported through the submodules retain temporary deprecated aliases in `pyo3::types`.
