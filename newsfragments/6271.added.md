`#[pymodule_init]` may now be written with no arguments. Such an initialiser provably cannot add attributes to the module, so the module is no longer tagged incomplete for `experimental-inspect`: its stubs get an `__all__` and no `__getattr__` catch-all.
